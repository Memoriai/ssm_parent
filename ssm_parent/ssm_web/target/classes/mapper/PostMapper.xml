<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.imain.dao.PostDao">
    <select id="aphorism" resultType="net.imain.domain.Post" parameterType="java.lang.Integer">
        select post from post_data where id = #{id}
    </select>
</mapper>